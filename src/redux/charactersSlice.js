import { createSlice } from "@reduxjs/toolkit";

export const chrarectersSlice = createSlice({
  name: "characters",

  initialState: {
    items: [],
  },

  reducers: {},
});

export default chrarectersSlice.reducer;
